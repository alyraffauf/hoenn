{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    "docker:pinDigests",
    ":dependencyDashboard",
    "helpers:pinGitHubActionDigests"
  ],
  "timezone": "America/New_York",
  "labels": ["dependencies"],
  "platformAutomerge": true,

  "nix": {
    "enabled": true
  },

  "kubernetes": {
    "managerFilePatterns": ["/k8s/.+\\.ya?ml$/"]
  },

  "flux": {
    "managerFilePatterns": ["/k8s/.+\\.ya?ml$/"]
  },

  "lockFileMaintenance": {
    "enabled": true,
    "automerge": true,
    "schedule": ["before 4am on the first day of the month"]
  },

  "packageRules": [
    {
      "description": "OCI Helm charts use chart versions, not container digests",
      "matchManagers": ["flux"],
      "pinDigests": false
    },
    {
      "description": "Auto-merge routine updates after CI passes",
      "matchUpdateTypes": ["minor", "patch", "digest", "pin", "pinDigest"],
      "automerge": true
    },
    {
      "description": "Major bumps stay manual",
      "matchUpdateTypes": ["major"],
      "automerge": false
    },
    {
      "description": "Update each declared flake input and its lock entries independently",
      "matchManagers": ["nix"],
      "groupName": null,
      "automerge": true
    },
    {
      "description": "Ignore FlakeHub inputs because Renovate cannot resolve their references",
      "matchManagers": ["nix"],
      "matchPackageNames": ["/^https:\\/\\/flakehub\\.com\\/f\\//"],
      "enabled": false
    }
  ],

  "vulnerabilityAlerts": {
    "enabled": true,
    "labels": ["security"]
  }
}
